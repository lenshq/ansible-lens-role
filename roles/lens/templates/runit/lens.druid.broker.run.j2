#!/bin/bash
#
# Generated by Ansible
# Local modification will be overwritten.
#

# Settings
APP_ROOT=/tmp/druid-0.8.1
APP_USER={{ lens_deploy_user.name }}
APP_GROUP={{ lens_deploy_user.name }}


CMD="java -Xmx256m -Duser.timezone=UTC -Dfile.encoding=UTF-8 -classpath config/_common:config/broker:lib/* io.druid.cli.Main server broker"
CHPSTKEY="-u ${APP_USER}:${APP_GROUP}"

cd ${APP_ROOT}

# Run
exec 2>&1
echo "Running lens druid broker server..."
exec chpst $CHPSTKEY $CMD
