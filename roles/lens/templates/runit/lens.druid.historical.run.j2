#!/bin/bash
#
# Generated by Ansible
# Local modification will be overwritten.
#

# Settings
APP_ROOT=/usr/local/bin/druid
APP_USER={{ lens_deploy_user.name }}
APP_GROUP={{ lens_deploy_user.name }}


CMD="java -Xmx256m -Duser.timezone=UTC -Dfile.encoding=UTF-8 -classpath config/_common:config/historical:lib/* io.druid.cli.Main server historical"
CHPSTKEY="-u ${APP_USER}:${APP_GROUP}"

cd ${APP_ROOT}

# Run
exec 2>&1
echo "Running lens druid historical server..."
exec chpst $CHPSTKEY $CMD
